<x-layouts.admin title="Events" heading="Events" subheading="Create, filter, and manage events">
    <section class="rounded-lg border border-slate-200 bg-white shadow-sm">
        <div class="flex flex-col gap-3 border-b border-slate-200 p-5 lg:flex-row lg:items-center lg:justify-between"><form method="GET" class="grid flex-1 gap-3 lg:grid-cols-[1fr_160px_180px_180px_150px_150px_auto]"><input name="search" value="{{ request('search') }}" placeholder="Search title or slug" class="min-h-11 rounded-lg border border-slate-300 px-3 text-sm"><select name="status_key" class="min-h-11 rounded-lg border border-slate-300 px-3 text-sm"><option value="">All status</option>@foreach(\App\Enums\EventLifecycleStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status_key') === $status->value)>{{ ucfirst($status->value) }}</option>@endforeach</select><select name="event_category_id" class="min-h-11 rounded-lg border border-slate-300 px-3 text-sm"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string)request('event_category_id')===(string)$category->id)>{{ $category->name }}</option>@endforeach</select><select name="venue_id" class="min-h-11 rounded-lg border border-slate-300 px-3 text-sm"><option value="">All venues</option>@foreach($venues as $venue)<option value="{{ $venue->id }}" @selected((string)request('venue_id')===(string)$venue->id)>{{ $venue->name }}</option>@endforeach</select><input type="date" name="date_from" value="{{ request('date_from') }}" class="min-h-11 rounded-lg border border-slate-300 px-3 text-sm"><input type="date" name="date_to" value="{{ request('date_to') }}" class="min-h-11 rounded-lg border border-slate-300 px-3 text-sm"><button class="btn btn-outline-primary btn-md">Filter</button></form><a href="{{ route('admin.events.create') }}" class="btn btn-primary btn-md">New event</a></div>
        <div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Event</th><th class="px-5 py-3">When</th><th class="px-5 py-3">Venue</th><th class="px-5 py-3">Capacity</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($events as $event)<tr><td class="px-5 py-4"><a href="{{ route('admin.events.show',$event) }}" class="font-semibold hover:text-emerald-700">{{ $event->title }}</a><p class="mt-1 text-slate-500">{{ $event->category?->name }} / {{ $event->type?->name }}</p></td><td class="px-5 py-4">{{ $event->starts_at->format('d M Y H:i') }}</td><td class="px-5 py-4">{{ $event->venue?->name ?? '-' }}</td><td class="px-5 py-4">{{ number_format($event->capacity) }}</td><td class="px-5 py-4">{{ ucfirst($event->status_key->value) }}</td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.events.edit',$event) }}" class="btn btn-outline-primary btn-sm">Edit</a><a href="{{ route('admin.events.preview',$event) }}" class="btn btn-outline-primary btn-sm">Preview</a></div></td></tr>@empty<tr><td colspan="6" class="px-5 py-8 text-center text-slate-500">No events found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 p-5">{{ $events->links() }}</div>
    </section>
</x-layouts.admin>
