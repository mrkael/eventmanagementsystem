<x-layouts.admin title="Event Configuration" heading="Event Configuration" subheading="Default registration, QR, capacity, and email behavior">
    <section class="rounded-lg border border-slate-200 bg-white shadow-sm">
        <div class="flex items-center justify-between border-b border-slate-200 p-5"><h2 class="text-sm font-semibold">Configurations</h2><a href="{{ route('admin.event-configurations.create') }}" class="inline-flex min-h-11 items-center rounded-lg bg-emerald-700 px-4 text-sm font-semibold text-white hover:bg-emerald-800">New configuration</a></div>
        <div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Name</th><th class="px-5 py-3">Default</th><th class="px-5 py-3">Registration</th><th class="px-5 py-3">QR</th><th class="px-5 py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($configurations as $configuration)<tr><td class="px-5 py-4 font-semibold">{{ $configuration->name }}</td><td class="px-5 py-4">{{ $configuration->is_default ? 'Yes' : 'No' }}</td><td class="px-5 py-4">{{ $configuration->registration_rules['allow_waitlist'] ? 'Waitlist enabled' : 'No waitlist' }}</td><td class="px-5 py-4">{{ $configuration->qr_rules['enabled'] ? 'Enabled' : 'Disabled' }}</td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.event-configurations.edit', $configuration) }}" class="rounded-lg border border-slate-300 px-3 py-2 font-semibold hover:bg-slate-50">Edit</a><form method="POST" action="{{ route('admin.event-configurations.destroy', $configuration) }}" onsubmit="return confirm('Delete this configuration?')">@csrf @method('DELETE')<button @disabled($configuration->is_default) class="rounded-lg border border-red-200 px-3 py-2 font-semibold text-red-700 hover:bg-red-50 disabled:cursor-not-allowed disabled:opacity-40">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="px-5 py-8 text-center text-slate-500">No configurations found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 p-5">{{ $configurations->links() }}</div>
    </section>
</x-layouts.admin>
