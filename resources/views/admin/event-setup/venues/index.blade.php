<x-layouts.admin title="Venues" heading="Venues" subheading="Manage locations and capacity">
    <section class="rounded-lg border border-slate-200 bg-white shadow-sm">
        <div class="flex flex-col gap-3 border-b border-slate-200 p-5 lg:flex-row lg:items-center lg:justify-between"><form method="GET" class="flex flex-1 flex-col gap-3 sm:flex-row"><input name="search" value="{{ request('search') }}" placeholder="Search venue, code, location" class="min-h-11 rounded-lg border border-slate-300 px-3 text-sm focus:border-emerald-600 focus:outline-none focus:ring-2 focus:ring-emerald-600/20"><select name="status" class="min-h-11 rounded-lg border border-slate-300 px-3 text-sm focus:border-emerald-600 focus:outline-none focus:ring-2 focus:ring-emerald-600/20"><option value="">All statuses</option>@foreach ($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ ucfirst($status->value) }}</option>@endforeach</select><button class="btn btn-outline-primary btn-md">Filter</button></form><a href="{{ route('admin.venues.create') }}" class="btn btn-primary btn-md">New venue</a></div>
        <div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Venue</th><th class="px-5 py-3">Capacity</th><th class="px-5 py-3">Location</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($venues as $venue)<tr><td class="px-5 py-4"><a href="{{ route('admin.venues.show', $venue) }}" class="font-semibold hover:text-emerald-700">{{ $venue->name }}</a><p class="mt-1 font-mono text-xs text-slate-500">{{ $venue->code }}</p></td><td class="px-5 py-4">{{ number_format($venue->capacity) }}</td><td class="px-5 py-4">{{ $venue->location ?: '-' }}</td><td class="px-5 py-4">{{ ucfirst($venue->status->value) }}</td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.venues.edit', $venue) }}" class="btn btn-outline-primary btn-sm">Edit</a><form method="POST" action="{{ route('admin.venues.destroy', $venue) }}" onsubmit="return confirm('Delete this venue?')">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="px-5 py-8 text-center text-slate-500">No venues found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 p-5">{{ $venues->links() }}</div>
    </section>
</x-layouts.admin>
